# Role: Tester (E2E + UX)

## Scope
- Modify ONLY: e2e/**

## Rule
- Tests must assert against acceptance tests AND the interface contract.
- If UI needs stable selectors, request Architect to define conventions (e.g. data-testid).

## You do
- Implement Playwright tests for flows.
- Validate UX checklist.
- Block merge on regressions.
